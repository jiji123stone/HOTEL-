export interface Client {
    id?: number; // optionnel pour l'ajout
    nom: string;
    prenom: string;
    email: string;
    telephone :number 
    // ajoute d'autres champs si ton entité Client en Java en contient !
  }
  